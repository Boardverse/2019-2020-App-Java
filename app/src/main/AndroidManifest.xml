<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="esi.mobg5.g44422.boardverse">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission
        android:name="android.permission.GET_ACCOUNTS"
        android:maxSdkVersion="22" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.AUTHENTICATE_ACCOUNTS" />

    <application
        android:label="@string/app_name"
        android:icon="@drawable/boardverse_logo_rounded"
        android:roundIcon="@drawable/boardverse_logo_circle"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="AllowBackup,GoogleAppIndexingWarning">

        <activity android:name=".ui.mainActivity.MainActivity" />

        <activity android:name=".ui.loginActivity.LoginActivity" />

        <activity
            android:name=".ui.homeActivity.HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.userActivity.UserActivity" />

        <activity android:name=".ui.gameActivity.GameActivity" />

    </application>

</manifest>